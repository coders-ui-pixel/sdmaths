# dependencies
/node_modules
/deploy/node_modules

# next.js build output
/.next
/deploy/.next
/out
tsconfig.tsbuildinfo

# secrets / env
.env
.env.*
!.env.example

# uploaded user content (served from disk, not source)
/public/uploads

# logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# misc local scratch
/deploy

# local AI tooling config (machine-specific, not project source)
/.claude
